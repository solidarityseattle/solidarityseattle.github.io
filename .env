# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# API Configuration, set to be /api
# VITE_API_URL=

# Backend secrets (generate your own values)
# Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# JWT_SECRET=

# Generate ADMIN_HASH with: node -e "const bcrypt = require('bcrypt'); bcrypt.hash('your_password', 10).then(hash => console.log(hash));"
# ADMIN_HASH=

# MongoDB connection string
# MONGODB_URI=
